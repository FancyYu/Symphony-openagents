# 🎻 Symphony 系统架构详解

## 📊 智能体协作流程

### 完整的消息处理流程

```
用户在频道发送消息: "最近工作压力很大"
         ↓
┌────────────────────────────────────────────────────────────┐
│ 第1步: Daily Assistant Agent 接收并回复                    │
│ - 类型: CollaboratorAgent (YAML配置)                       │
│ - 监听: 所有频道消息 (react_to_all_messages: true)         │
│ - 行为: 立即回复用户，提供情感支持                          │
│ - 回复示例: "我理解你的压力。让我帮你深入分析一下。"        │
└────────────────────────────────────────────────────────────┘
         ↓
┌────────────────────────────────────────────────────────────┐
│ 第2步: Daily Assistant Listener 监听并检测                 │
│ - 类型: WorkerAgent (Python)                               │
│ - 监听: 所有频道消息                                        │
│ - 行为:                                                     │
│   1. 提取消息内容                                           │
│   2. 检测关键词 ("压力" 匹配!)                              │
│   3. 识别框架 (general/MBTI/HUMAN 3.0等)                   │
│   4. 保存消息到数据库                                       │
│   5. 发送分析请求给 Analyst Agent                           │
└────────────────────────────────────────────────────────────┘
         ↓
┌────────────────────────────────────────────────────────────┐
│ 第3步: Analyst Agent 执行分析                              │
│ - 类型: WorkerAgent (Python)                               │
│ - 接收: 来自 Listener 的分析请求                            │
│ - 行为:                                                     │
│   1. 从 Memory Palace 获取用户上下文                        │
│   2. 从 Framework Library 获取分析框架                      │
│   3. 使用 LLM 生成 3-5 个洞察                               │
│   4. 保存分析结果到数据库                                   │
│   5. 保存到 Memory Palace (长期记忆)                        │
│   6. 更新用户画像                                           │
│   7. 发送分析结果给 Creator Agent                           │
└────────────────────────────────────────────────────────────┘
         ↓
┌────────────────────────────────────────────────────────────┐
│ 第4步: Creator Agent 生成行动计划                          │
│ - 类型: WorkerAgent (Python)                               │
│ - 接收: 来自 Analyst 的分析结果                             │
│ - 行为:                                                     │
│   1. 基于洞察生成行动计划                                   │
│   2. 结构化计划 (标题、概述、步骤、时间线)                  │
│   3. 保存行动计划到数据库                                   │
│   4. 格式化为用户友好的消息                                 │
│   5. 发送给用户 (通过频道或直接消息)                        │
└────────────────────────────────────────────────────────────┘
         ↓
用户收到完整的行动计划 🎯
```

---

## 🔗 智能体间通信机制

### 1. 消息传递方式

#### 方式 1: Direct Message (直接消息)

```python
# Listener → Analyst
messaging = self.client.mod_adapters.get("openagents.mods.workspace.messaging")
await messaging.send_direct_message(
    target_agent_id="analyst-agent",
    text=f"ANALYSIS_REQUEST|{user_id}|{framework}|{content}"
)

# Analyst → Creator
await messaging.send_direct_message(
    target_agent_id="creator-agent",
    text=f"ANALYSIS_COMPLETED|{json.dumps(analysis_data)}"
)
```

#### 方式 2: Event System (事件系统)

```python
# 发布事件
await self.client.send_event(
    event_type="analysis.requested",
    target_agent_id="analyst-agent",
    payload={
        "user_id": user_id,
        "content": content,
        "framework": framework
    }
)
```

### 2. 消息格式

#### Listener → Analyst

```
格式: "ANALYSIS_REQUEST|{user_id}|{framework}|{content}"
示例: "ANALYSIS_REQUEST|admin|general|最近工作压力很大"
```

#### Analyst → Creator

```
格式: "ANALYSIS_COMPLETED|{json_data}"
示例: "ANALYSIS_COMPLETED|{"user_id":"admin","framework":"general","insights":[...]}"
```

---

## 💾 数据互通机制

### 1. 共享数据库

所有智能体都可以访问相同的数据库：

```python
# 所有 agent 都导入
from storage.simple_storage import storage
from storage.memory_palace import memory_palace
from storage.framework_library import framework_library

# Listener 保存消息
storage.save_message(user_id, content, ...)

# Analyst 读取历史
history = storage.get_user_messages(user_id, limit=10)

# Analyst 保存分析
storage.save_analysis(user_id, framework, insights, ...)

# Creator 读取分析
# (通过消息传递，不需要直接读取)

# Creator 保存行动计划
storage.save_action_plan(user_id, title, steps, ...)
```

### 2. Memory Palace 共享

```python
# Analyst 写入长期记忆
memory_palace.add_long_term_memory(
    user_id=user_id,
    memory_type="analysis",
    content=insights,
    keywords=keywords,
    importance=0.8
)

# Analyst 读取上下文
context = memory_palace.build_context(user_id, current_topic=content)

# 更新用户画像
memory_palace.add_framework_usage(user_id, framework)
```

### 3. Framework Library 共享

```python
# Analyst 获取框架
framework = framework_library.get_framework("MBTI")

# 获取分析提示
prompt = framework_library.get_analysis_prompt("MBTI")

# 搜索合适的框架
framework_name = framework_library.search_framework(user_message)
```

---

## 👤 谁在接收和回复？

### 场景 1: 用户发送普通消息

```
用户: "你好"
  ↓
Daily Assistant 接收 ✅
  - 检测到: 普通问候
  - 行为: 直接回复 "你好！有什么我可以帮助你的吗？"
  ↓
Listener 监听 ✅
  - 检测到: 无分析关键词
  - 行为: 仅保存消息，不触发分析
  ↓
Analyst: 不参与 ❌
Creator: 不参与 ❌
```

### 场景 2: 用户发送需要分析的消息

```
用户: "最近工作压力很大，帮我分析一下"
  ↓
Daily Assistant 接收 ✅
  - 检测到: 压力相关
  - 行为: 回复 "我理解你的压力。让我连接分析师为你提供深入洞察。"
  ↓
Listener 监听 ✅
  - 检测到: "压力"、"分析" 关键词
  - 行为:
    1. 保存消息
    2. 识别框架 (general)
    3. 发送分析请求给 Analyst
  ↓
Analyst 接收分析请求 ✅
  - 行为:
    1. 获取用户上下文
    2. 使用 LLM 分析
    3. 生成洞察
    4. 保存结果
    5. 发送给 Creator
  ↓
Creator 接收分析结果 ✅
  - 行为:
    1. 生成行动计划
    2. 格式化消息
    3. 发送给用户
  ↓
用户收到: 完整的分析报告和行动计划 🎯
```

### 场景 3: 用户指定框架

```
用户: "用 MBTI 分析我的性格"
  ↓
Daily Assistant 接收 ✅
  - 回复: "好的，我会使用 MBTI 框架为你分析。"
  ↓
Listener 监听 ✅
  - 检测到: "MBTI"、"性格"、"分析"
  - 识别框架: MBTI (而不是 general)
  - 发送分析请求 (framework="MBTI")
  ↓
Analyst 使用 MBTI 框架分析 ✅
  - 从 Framework Library 获取 MBTI 框架
  - 使用 MBTI 的维度和提示
  - 生成 MBTI 相关洞察
  ↓
Creator 生成 MBTI 相关行动计划 ✅
  ↓
用户收到: MBTI 分析报告 🎯
```

---

## 🔄 数据流图

```
┌─────────────────────────────────────────────────────────────┐
│                        用户消息                              │
└─────────────────────────────────────────────────────────────┘
                           ↓
        ┌──────────────────┴──────────────────┐
        ↓                                      ↓
┌───────────────┐                    ┌────────────────┐
│ Daily         │                    │ Daily          │
│ Assistant     │ 立即回复用户        │ Assistant      │
│ (YAML)        │ ─────────────→     │ Listener       │
└───────────────┘                    │ (Python)       │
                                     └────────────────┘
                                              ↓
                                     检测关键词 + 识别框架
                                              ↓
                                     保存到 Simple Storage
                                              ↓
                                     发送分析请求
                                              ↓
                                     ┌────────────────┐
                                     │ Analyst        │
                                     │ Agent          │
                                     │ (Python)       │
                                     └────────────────┘
                                              ↓
                    ┌─────────────────────────┼─────────────────────────┐
                    ↓                         ↓                         ↓
            ┌───────────────┐        ┌───────────────┐        ┌───────────────┐
            │ Memory        │        │ Framework     │        │ Simple        │
            │ Palace        │        │ Library       │        │ Storage       │
            │               │        │               │        │               │
            │ - 获取上下文   │        │ - 获取框架    │        │ - 读取历史    │
            │ - 保存记忆    │        │ - 获取提示    │        │ - 保存分析    │
            │ - 更新画像    │        │               │        │               │
            └───────────────┘        └───────────────┘        └───────────────┘
                    ↑                         ↑                         ↑
                    └─────────────────────────┴─────────────────────────┘
                                              ↓
                                     使用 LLM 生成洞察
                                              ↓
                                     发送分析结果
                                              ↓
                                     ┌────────────────┐
                                     │ Creator        │
                                     │ Agent          │
                                     │ (Python)       │
                                     └────────────────┘
                                              ↓
                                     生成行动计划
                                              ↓
                                     保存到 Simple Storage
                                              ↓
                                     发送给用户
                                              ↓
                                     ┌────────────────┐
                                     │ 用户收到       │
                                     │ 完整报告       │
                                     └────────────────┘
```

---

## 🎯 关键设计特点

### 1. 并行处理

- Daily Assistant 和 Listener **同时**监听消息
- Daily Assistant 提供即时反馈
- Listener 在后台触发深度分析

### 2. 解耦设计

- 每个 agent 独立运行
- 通过消息传递通信
- 共享数据库访问

### 3. 数据持久化

- 所有交互保存到数据库
- 支持历史查询和上下文构建
- 用户画像持续更新

### 4. 智能路由

- Listener 自动检测是否需要分析
- 自动识别合适的分析框架
- 避免不必要的 LLM 调用

### 5. 上下文感知

- Analyst 访问用户历史
- 使用 Memory Palace 构建上下文
- 个性化分析

---

## 📝 实际示例

### 示例 1: 完整流程

**用户输入**: "我最近工作压力很大，经常焦虑"

**Daily Assistant 回复** (立即):

```
我理解你的感受。工作压力和焦虑是很常见的问题。
让我为你进行深入分析，找出压力来源和应对方法。
```

**后台处理** (2-5 秒):

1. Listener 检测到 "压力"、"焦虑" 关键词
2. 识别框架: general
3. Analyst 分析:
   - 获取用户历史对话
   - 使用 general 框架
   - 生成洞察
4. Creator 生成行动计划

**最终回复** (用户收到):

```
🎯 压力管理与情绪调节计划

📝 基于你的情况，我为你准备了以下洞察和行动计划：

💡 关键洞察：
1. 工作压力主要来源于任务过载和时间管理
2. 焦虑情绪可能与完美主义倾向相关
3. 需要建立更好的工作生活边界

📋 行动计划：

1. 优先级管理 (本周)
   💡 使用艾森豪威尔矩阵整理任务

2. 每日放松练习 (每天)
   💡 减少焦虑，提升情绪稳定性

3. 设定工作边界 (本月)
   💡 改善工作生活平衡

🌟 开始行动吧！如果需要调整或有任何问题，随时告诉我。
```

---

## 🔧 技术实现细节

### Agent 启动顺序

```bash
# 1. 启动网络服务
openagents network start .

# 2. 启动 Daily Assistant (YAML)
openagents agent start configs/daily_assistant.yaml

# 3. 启动 Listener (Python)
python agents/daily_assistant_listener.py

# 4. 启动 Analyst (Python)
python agents/analyst_agent.py

# 5. 启动 Creator (Python)
python agents/creator_agent.py
```

### 通信端口

- HTTP: 8700 (Studio 访问)
- gRPC: 8600 (Agent 通信)

### 数据库文件

- `data/symphony_mvp.db` - Simple Storage
- `data/memory_palace.db` - Memory Palace

---

## ✅ 总结

### 谁在接收？

- **Daily Assistant**: 接收所有消息，提供即时回复
- **Listener**: 监听所有消息，检测分析需求
- **Analyst**: 接收分析请求（来自 Listener）
- **Creator**: 接收分析结果（来自 Analyst）

### 谁在回复？

- **Daily Assistant**: 立即回复用户（情感支持）
- **Creator**: 发送最终的分析报告和行动计划

### 数据如何互通？

1. **共享数据库**: 所有 agent 访问相同的 SQLite 数据库
2. **消息传递**: Agent 之间通过直接消息传递数据
3. **Memory Palace**: 共享的记忆系统，所有 agent 可读写
4. **Framework Library**: 共享的框架知识库

### 协作优势

- ✅ 即时反馈 + 深度分析
- ✅ 专业分工，各司其职
- ✅ 数据持久化，上下文感知
- ✅ 可扩展，易于添加新 agent
