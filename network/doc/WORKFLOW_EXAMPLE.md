# 🎯 Symphony 工作流程实例

## 完整示例：从用户消息到行动计划

---

## 📝 示例 1: 压力管理分析

### 用户输入

```
"最近工作压力很大，经常加班到很晚，感觉很焦虑"
```

### 处理流程

#### ⏱️ T+0 秒: Daily Assistant 立即响应

**接收者**: Daily Assistant Agent (YAML)  
**触发**: `react_to_all_messages: true`

**处理逻辑**:

```yaml
# configs/daily_assistant.yaml
instruction: |
  You are the Daily Assistant in Symphony system.
  Listen to users' daily updates, thoughts, and concerns.
  Be empathetic and supportive.
  Detect when deeper analysis is needed.
```

**回复内容**:

```
我理解你的感受。工作压力和焦虑确实很困扰人。
让我帮你深入分析一下压力来源和应对方法。
我会连接我们的分析师为你提供专业的洞察。
```

**数据库操作**: 无（仅回复）

---

#### ⏱️ T+0.5 秒: Listener 检测并触发分析

**接收者**: Daily Assistant Listener (Python)  
**触发**: 监听所有频道消息

**处理逻辑**:

```python
# agents/daily_assistant_listener.py

# 1. 提取消息
content = "最近工作压力很大，经常加班到很晚，感觉很焦虑"

# 2. 检测关键词
keywords_found = ["压力", "焦虑"]  # 匹配成功！

# 3. 识别框架
framework = _detect_framework(content)
# 结果: "general" (因为没有特定框架关键词)

# 4. 保存消息
storage.save_message(
    user_id="admin",
    content=content,
    message_type="channel_message",
    metadata={"source": "admin", "channel": "general"}
)
```

**数据库操作**:

```sql
INSERT INTO user_messages (user_id, content, message_type, metadata, timestamp)
VALUES ('admin', '最近工作压力很大...', 'channel_message', '{"source":"admin"}', '2026-01-14 17:30:00');
```

**发送消息**:

```python
# 发送给 Analyst Agent
await messaging.send_direct_message(
    target_agent_id="analyst-agent",
    text="ANALYSIS_REQUEST|admin|general|最近工作压力很大，经常加班到很晚，感觉很焦虑"
)
```

**控制台输出**:

```
📨 收到消息 from admin
   用户: admin
   内容: 最近工作压力很大，经常加班到很晚，感觉很焦虑
   🎯 检测到需要分析的内容
   📤 已发送分析请求给分析师智能体
```

---

#### ⏱️ T+1 秒: Analyst 执行深度分析

**接收者**: Analyst Agent (Python)  
**触发**: 接收到 "ANALYSIS_REQUEST|..." 消息

**处理逻辑**:

```python
# agents/analyst_agent.py

# 1. 解析请求
user_id = "admin"
framework = "general"
content = "最近工作压力很大，经常加班到很晚，感觉很焦虑"

# 2. 获取用户上下文
context = memory_palace.build_context(user_id, current_topic=content)
# 返回:
# {
#   "user_id": "admin",
#   "profile": {...},
#   "recent_memories": [...],
#   "relevant_long_term": [...]
# }

# 3. 获取分析框架
framework_obj = framework_library.get_framework("general")
# 返回:
# AnalysisFramework(
#   name="General Growth",
#   dimensions=["当前状态", "挑战与障碍", "优势与资源", ...],
#   ...
# )

# 4. 构建 LLM 提示
prompt = f"""
Analyze the following user message using the General Growth framework.

Framework Description: 通用个人成长分析框架，适用于各种成长话题

Framework Dimensions:
- 当前状态 (Current State)
- 挑战与障碍 (Challenges)
- 优势与资源 (Strengths)
- 成长机会 (Opportunities)
- 行动方向 (Action Direction)

User message: 最近工作压力很大，经常加班到很晚，感觉很焦虑

User Context:
- Total interactions: 3
- Previous frameworks used: []

Generate 3-5 key insights in Chinese.
"""

# 5. 调用 LLM (Groq)
response = await self.run_agent(prompt)
# LLM 返回:
"""
1. 工作压力的核心来源是时间管理和任务优先级问题，长时间加班表明工作负荷超出合理范围
2. 焦虑情绪是身体发出的警告信号，提示需要调整工作节奏和建立更好的边界
3. 建议采用番茄工作法和任务分解技术，提高工作效率，减少不必要的加班
4. 需要建立每日放松练习，如深呼吸、冥想或轻度运动，帮助缓解焦虑
5. 考虑与上级沟通工作量问题，寻求支持或资源调整
"""

# 6. 解析洞察
insights = [
    "工作压力的核心来源是时间管理和任务优先级问题，长时间加班表明工作负荷超出合理范围",
    "焦虑情绪是身体发出的警告信号，提示需要调整工作节奏和建立更好的边界",
    "建议采用番茄工作法和任务分解技术，提高工作效率，减少不必要的加班",
    "需要建立每日放松练习，如深呼吸、冥想或轻度运动，帮助缓解焦虑",
    "考虑与上级沟通工作量问题，寻求支持或资源调整"
]
```

**数据库操作**:

```sql
-- Simple Storage
INSERT INTO analysis_results (user_id, framework, insights, confidence, timestamp)
VALUES ('admin', 'general', '["工作压力的核心来源...", ...]', 0.8, '2026-01-14 17:30:02');

-- Memory Palace: 长期记忆
INSERT INTO long_term_memory (user_id, memory_type, content, keywords, importance, created_at)
VALUES ('admin', 'analysis', 'Framework: general\nInsights: [...]',
        'general,压力,工作,焦虑,加班', 0.8, '2026-01-14 17:30:02');

-- Memory Palace: 更新用户画像
UPDATE user_profiles
SET frameworks_used = '["general"]',
    updated_at = '2026-01-14 17:30:02'
WHERE user_id = 'admin';
```

**发送消息**:

```python
# 发送给 Creator Agent
analysis_data = {
    "user_id": "admin",
    "framework": "general",
    "insights": insights,
    "confidence": 0.8,
    "original_content": content
}

await messaging.send_direct_message(
    target_agent_id="creator-agent",
    text=f"ANALYSIS_COMPLETED|{json.dumps(analysis_data, ensure_ascii=False)}"
)
```

**控制台输出**:

```
📊 收到分析请求
   用户: admin
   框架: general
   内容: 最近工作压力很大，经常加班到很晚，感觉很焦虑
   ✅ 分析完成: 5 个洞察
   📤 已发送分析结果给创作者智能体
```

---

#### ⏱️ T+3 秒: Creator 生成行动计划

**接收者**: Creator Agent (Python)  
**触发**: 接收到 "ANALYSIS_COMPLETED|..." 消息

**处理逻辑**:

```python
# agents/creator_agent.py

# 1. 解析分析结果
analysis_data = {
    "user_id": "admin",
    "framework": "general",
    "insights": [
        "工作压力的核心来源是时间管理和任务优先级问题...",
        "焦虑情绪是身体发出的警告信号...",
        "建议采用番茄工作法和任务分解技术...",
        "需要建立每日放松练习...",
        "考虑与上级沟通工作量问题..."
    ],
    "confidence": 0.8,
    "original_content": "最近工作压力很大..."
}

# 2. 构建 LLM 提示
prompt = f"""
Based on the following analysis insights, create a practical action plan.

Framework: general

Key Insights:
1. 工作压力的核心来源是时间管理和任务优先级问题...
2. 焦虑情绪是身体发出的警告信号...
3. 建议采用番茄工作法和任务分解技术...
4. 需要建立每日放松练习...
5. 考虑与上级沟通工作量问题...

Create an action plan with:
1. Title: A motivating, clear title
2. Overview: Brief summary
3. Steps: 3-7 actionable steps with timeline and benefit

Format as JSON.
"""

# 3. 调用 LLM
response = await self.run_agent(prompt)
# LLM 返回 (简化版):
action_plan = {
    "title": "压力管理与工作效率提升计划",
    "overview": "通过时间管理、情绪调节和沟通技巧，系统性地降低工作压力，提升生活质量",
    "steps": [
        {
            "action": "实施番茄工作法，每25分钟专注工作后休息5分钟",
            "timeline": "本周开始",
            "benefit": "提高工作效率，减少加班时间"
        },
        {
            "action": "每天进行10分钟深呼吸或冥想练习",
            "timeline": "每日早晚",
            "benefit": "缓解焦虑，提升情绪稳定性"
        },
        {
            "action": "使用艾森豪威尔矩阵整理任务优先级",
            "timeline": "每周一",
            "benefit": "明确重点，避免被琐事淹没"
        },
        {
            "action": "与上级预约一对一会议，讨论工作量",
            "timeline": "本周内",
            "benefit": "获得支持，调整工作负荷"
        },
        {
            "action": "设定工作边界，晚上8点后不处理工作邮件",
            "timeline": "本月开始",
            "benefit": "改善工作生活平衡，保护个人时间"
        }
    ]
}

# 4. 格式化消息
message = f"""
🎯 {action_plan['title']}

📝 {action_plan['overview']}

📋 行动计划:

1. 实施番茄工作法，每25分钟专注工作后休息5分钟 (本周开始)
   💡 提高工作效率，减少加班时间

2. 每天进行10分钟深呼吸或冥想练习 (每日早晚)
   💡 缓解焦虑，提升情绪稳定性

3. 使用艾森豪威尔矩阵整理任务优先级 (每周一)
   💡 明确重点，避免被琐事淹没

4. 与上级预约一对一会议，讨论工作量 (本周内)
   💡 获得支持，调整工作负荷

5. 设定工作边界，晚上8点后不处理工作邮件 (本月开始)
   💡 改善工作生活平衡，保护个人时间

🌟 开始行动吧！如果需要调整或有任何问题，随时告诉我。
"""
```

**数据库操作**:

```sql
INSERT INTO action_plans (user_id, title, steps, created_at)
VALUES ('admin', '压力管理与工作效率提升计划',
        '{"overview":"...","steps":[...]}', '2026-01-14 17:30:04');
```

**发送消息**:

```python
# 发送给用户 (通过频道)
await messaging.send_channel_message(
    channel="general",
    text=message
)
```

**控制台输出**:

```
📋 收到分析结果
   用户: admin
   框架: general
   洞察数量: 5
   ✅ 行动计划已创建并发送给用户
   📤 标题: 压力管理与工作效率提升计划
```

---

#### ⏱️ T+4 秒: 用户收到完整报告

**用户在 Studio 看到**:

```
🎯 压力管理与工作效率提升计划

📝 通过时间管理、情绪调节和沟通技巧，系统性地降低工作压力，提升生活质量

📋 行动计划:

1. 实施番茄工作法，每25分钟专注工作后休息5分钟 (本周开始)
   💡 提高工作效率，减少加班时间

2. 每天进行10分钟深呼吸或冥想练习 (每日早晚)
   💡 缓解焦虑，提升情绪稳定性

3. 使用艾森豪威尔矩阵整理任务优先级 (每周一)
   💡 明确重点，避免被琐事淹没

4. 与上级预约一对一会议，讨论工作量 (本周内)
   💡 获得支持，调整工作负荷

5. 设定工作边界，晚上8点后不处理工作邮件 (本月开始)
   💡 改善工作生活平衡，保护个人时间

🌟 开始行动吧！如果需要调整或有任何问题，随时告诉我。
```

---

## 📊 数据流总结

### 数据库变化

**user_messages 表**:

```
| id | user_id | content                    | timestamp           |
|----|---------|----------------------------|---------------------|
| 1  | admin   | 最近工作压力很大...         | 2026-01-14 17:30:00 |
```

**analysis_results 表**:

```
| id | user_id | framework | insights (JSON)           | timestamp           |
|----|---------|-----------|---------------------------|---------------------|
| 1  | admin   | general   | ["工作压力的核心...", ...] | 2026-01-14 17:30:02 |
```

**action_plans 表**:

```
| id | user_id | title                      | steps (JSON)        | created_at          |
|----|---------|----------------------------|---------------------|---------------------|
| 1  | admin   | 压力管理与工作效率提升计划  | {"overview":..., ...} | 2026-01-14 17:30:04 |
```

**long_term_memory 表** (Memory Palace):

```
| id | user_id | memory_type | content              | keywords                    | importance |
|----|---------|-------------|----------------------|-----------------------------|------------|
| 1  | admin   | analysis    | Framework: general... | general,压力,工作,焦虑,加班 | 0.8        |
```

**user_profiles 表** (Memory Palace):

```
| user_id | frameworks_used | updated_at          |
|---------|-----------------|---------------------|
| admin   | ["general"]     | 2026-01-14 17:30:02 |
```

---

## 🔄 消息传递链

```
用户消息
  ↓
Daily Assistant (立即回复)
  ↓
Listener (检测 + 保存)
  ↓
"ANALYSIS_REQUEST|admin|general|最近工作压力很大..."
  ↓
Analyst (分析 + 保存)
  ↓
"ANALYSIS_COMPLETED|{user_id:admin, insights:[...], ...}"
  ↓
Creator (生成计划 + 保存)
  ↓
发送给用户 (频道消息)
  ↓
用户收到完整报告
```

---

## ⏱️ 时间线

| 时间   | Agent           | 操作             | 耗时  |
| ------ | --------------- | ---------------- | ----- |
| T+0s   | Daily Assistant | 接收消息并回复   | ~0.5s |
| T+0.5s | Listener        | 检测关键词并触发 | ~0.5s |
| T+1s   | Analyst         | 获取上下文       | ~0.5s |
| T+1.5s | Analyst         | LLM 分析         | ~1.5s |
| T+3s   | Creator         | LLM 生成计划     | ~1s   |
| T+4s   | 用户            | 收到完整报告     | -     |

**总耗时**: ~4 秒（从用户发送到收到报告）

---

## 💡 关键要点

1. **并行处理**: Daily Assistant 和 Listener 同时工作
2. **即时反馈**: 用户立即得到情感支持
3. **深度分析**: 后台进行专业分析
4. **数据持久化**: 所有交互都被保存
5. **上下文感知**: 使用历史数据提供个性化分析
6. **可追溯**: 完整的数据链路，可以回溯任何分析

---

## 🎯 用户体验

从用户角度看：

1. 发送消息后立即得到回复（不用等待）
2. 几秒后收到专业的分析报告
3. 获得可操作的行动计划
4. 感受到系统的智能和关怀

从系统角度看：

1. 高效的任务分工
2. 完整的数据记录
3. 智能的上下文管理
4. 可扩展的架构
